# Web discovery log

Date searched: 2026-08-20 (Asia/Shanghai).

Parallel Web CLI 0.9.2 was installed as requested by the literature-review workflow, but `parallel-cli auth` reported `Not authenticated`; no API key was present. No query was silently omitted: broad discovery was executed with the built-in web search and formal academic exports were run separately through arXiv, OpenAlex, Semantic Scholar, and Crossref APIs.

## Built-in web search batch 1

1. `"LLM agent" guardrails runtime policy enforcement paper GitHub`
2. `"language model agents" tool call validation authorization paper`
3. `"agent harness" instruction compliance GitHub paper`
4. `"LLM agent" runtime monitor invariant policy enforcement`

Notable primary candidates discovered: HarnessAudit, Tool Forge, Code as Agent Harness, VeriGuard, AgentSpec, Agent-C, GuardAgent, Governance by Construction/CUGA, plus engineering-only harness repositories. Reddit and aggregator results were retained only for discovery and are not treated as academic evidence.

## Built-in web search batch 2

1. `site:arxiv.org LLM agents runtime enforcement safe policy tool calls AgentSpec Progent`
2. `site:arxiv.org LLM agent capability security sandbox authorization IsolateGPT CaMeL`
3. `site:openreview.net LLM agent guardrails runtime monitor tool use policy`
4. `site:dl.acm.org LLM agent policy enforcement tool runtime guardrails`

Notable primary candidates discovered: Progent, AgentSpec, IsolateGPT, VeriGuard, GuardAgent, AGrail and formal runtime-monitoring work. Pure monitor-persuasion studies, output-only guardian models and general safety surveys are screened separately rather than assumed to be runtime enforcement systems.
