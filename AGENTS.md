# Repository handoff policy

完成每一次用户任务后，必须把本任务产生且适合长期保存的内容提交到当前 Git 仓库并推送到其跟踪远程分支。

提交前：

1. 检查 `git status`，只纳入报告、代码、论文、数据索引、图表、测试和必要配置等交付内容。
2. 排除浏览器 profile、缓存、临时渲染、构建产物、一次性解析工具、日志、凭据和其他可重建噪声；必要时更新 `.gitignore`。
3. 不覆盖或回滚用户已有改动；不使用强制推送。
4. 运行与改动相称的验证，并检查暂存差异。
5. 使用清晰的提交消息提交，然后推送当前分支。最终答复中报告 commit、分支、远程和未提交例外。

若远程拒绝、认证失败或存在需要人工解决的历史冲突，应保留本地提交并向用户明确报告，不得强制覆盖远程。

# 用户协作偏好

- 在任务目标和授权范围明确后，持续执行到完成，不为普通中间步骤要求用户回复确认。
- 只有遇到确实需要用户批准、授权或会实质改变结果的关键选择时才暂停；暂停时直接提供清晰、互斥的选项供用户选择。
